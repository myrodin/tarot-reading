# 🔮 신비로운 타로 - Tarot Reading Website

아름다운 애니메이션과 함께하는 인터랙티브 타로점 웹사이트입니다.

## ✨ 주요 기능

- 🃏 **22장의 메이저 아르카나 카드**
- 🎭 **3D 카드 뒤집기 애니메이션**
- 🌟 **원카드 리딩**: 오늘의 운세
- 🔮 **쓰리카드 스프레드**: 과거/현재/미래
- 📖 **상세한 카드 해석** (정방향/역방향)
- ⭐ **아름다운 UI/UX** (신비로운 분위기)

## 🛠️ 기술 스택

- **React 18** - UI 라이브러리
- **Vite** - 빠른 빌드 도구
- **Framer Motion** - 부드러운 애니메이션
- **React Router** - 페이지 라우팅
- **Tailwind CSS v4** - 모던 스타일링

## 🚀 로컬 개발

### 설치
```bash
npm install
```

### 개발 서버 실행
```bash
npm run dev
```

브라우저에서 `http://localhost:5173` 접속

### 빌드
```bash
npm run build
```

## 📦 GitHub Pages 배포

### 방법 1: 수동 배포

```bash
npm run deploy
```

### 방법 2: 자동 배포 (GitHub Actions)

1. **GitHub 저장소 생성**
   - GitHub에서 새 저장소 생성 (예: `tarot-reading`)

2. **저장소에 코드 푸시**
   ```bash
   git init
   git add .
   git commit -m "Initial commit: Tarot reading website"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/tarot-reading.git
   git push -u origin main
   ```

3. **GitHub Pages 설정**
   - 저장소 Settings → Pages
   - Source: "GitHub Actions" 선택

4. **자동 배포**
   - `main` 브랜치에 푸시할 때마다 자동으로 배포됩니다
   - Actions 탭에서 배포 진행 상황 확인 가능

5. **사이트 접속**
   - `https://YOUR_USERNAME.github.io/tarot-reading/`

### 중요: base URL 설정

`vite.config.js`에서 저장소 이름에 맞게 `base` 설정:

```js
export default defineConfig({
  base: '/tarot-reading/', // 저장소 이름과 동일하게
})
```

저장소 이름이 다르면 여기를 수정하세요!

## 📁 프로젝트 구조

```
tarot-reading/
├── src/
│   ├── components/
│   │   ├── Card.jsx           # 3D 카드 컴포넌트
│   │   ├── CardDeck.jsx       # 카드 선택 인터페이스
│   │   └── Interpretation.jsx # 해석 화면
│   ├── pages/
│   │   ├── Home.jsx           # 랜딩 페이지
│   │   └── Reading.jsx        # 리딩 페이지
│   ├── data/
│   │   └── tarotCards.json    # 카드 데이터
│   ├── App.jsx                # 메인 앱
│   └── index.css              # 스타일
├── .github/
│   └── workflows/
│       └── deploy.yml         # 자동 배포 설정
└── vite.config.js             # Vite 설정
```

## 🎨 커스터마이징

### 카드 데이터 수정
`src/data/tarotCards.json`에서 카드 정보 수정 가능

### 색상 테마 변경
`tailwind.config.js`에서 커스텀 색상 수정:
- `mystic-purple`: 신비로운 보라색
- `mystic-blue`: 심해 블루
- `cosmic-gold`: 우주의 금빛

### 애니메이션 조정
각 컴포넌트의 Framer Motion 설정 수정

## 📄 라이선스

MIT License - 자유롭게 사용하세요!

## 🌙 즐기세요!

직관을 믿고 카드의 메시지에 귀 기울여보세요 ✨
